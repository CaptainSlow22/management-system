server:
  port: 8080

spring:
  datasource:
    username: postgres
    password: catalin1975
    url: jdbc:postgresql://localhost:5432/managementsys
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
